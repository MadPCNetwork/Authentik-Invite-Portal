# Authentik OIDC Configuration
AUTHENTIK_ISSUER_URL=https://auth.example.com/application/o/invite-portal/
AUTHENTIK_CLIENT_ID=your-client-id
AUTHENTIK_CLIENT_SECRET=your-client-secret
AUTHENTIK_FLOW_SLUG=default-enrollment-flow

# Authentik API Configuration (Service Account Token)
AUTHENTIK_API_URL=https://auth.example.com
AUTHENTIK_API_TOKEN=your-api-token

# Admin Configuration
ADMIN_GROUP_NAME=Invite Portal Admins

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-a-secure-secret-here

# Database (SQLite path for Docker volume mount)
DATABASE_URL="file:./data/invite-portal.db"

# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=user@example.com
SMTP_PASSWORD=password
SMTP_USE_TLS=true
SMTP_FROM_EMAIL=noreply@authentik.example.com
APP_NAME=Authentik
